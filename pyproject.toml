[project]
name = "fastapi-cicd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentscope==1.0.11",
    "agentscope-runtime==1.0.4",
]
[tool.ruff]
extend-select = ["I"]
